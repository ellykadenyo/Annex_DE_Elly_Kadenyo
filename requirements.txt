# Locked to the versions exercised during development on Python 3.13.
# Use `pip install -r requirements.txt` inside a fresh virtualenv.
pandas>=2.2,<3.0
numpy>=1.26,<3.0
openpyxl>=3.1,<4.0
duckdb>=1.0,<2.0
pyarrow>=17.0,<22.0
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0
PyYAML>=6.0,<7.0
reportlab>=4.0,<5.0
pytest>=8.0,<9.0
